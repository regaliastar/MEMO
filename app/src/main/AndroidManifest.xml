<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.memo3">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE">

    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rem"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.studyList"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".activity.amuseList"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".activity.liveList"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".activity.memoContent"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".activity.saveContent"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".test.Test">

        </activity>
        <activity android:name=".web.webView">

        </activity>
        <activity android:name=".activity.takePhoto">

        </activity>
    </application>

</manifest>